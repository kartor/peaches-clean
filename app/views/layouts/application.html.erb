<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Peaches & Clean</title>
    <%= csrf_meta_tags %>
		<link href="//fonts.googleapis.com/css?family=Lobster|Quicksand:400,500,700" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">

        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-nav-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to root_url, class: "navbar-brand" do %>
            Peaches & Clean
          <% end %>
        </div>

        <div class="collapse navbar-collapse" id="main-nav-collapse">
          <ul class="nav navbar-nav navbar-right">
            <!--
            <li><%= link_to "About", about_url %></li>
            -->
            <li><a href="//peachesandclean.net/contact"><span class="glyphicon glyphicon-envelope" aria-hidden="true"></span> Contact</a></li>
            <!--
            <%= link_to "Contact", new_contact_url %>
            <% if user_signed_in? %>
              <li><%= link_to "Log Out", destroy_user_session_url, method: :delete %></li>
            <% else %>
              <li><%= link_to "Log In", new_user_session_url %></li>
              <li><%= link_to "Sign Up", new_user_registration_url %></li>
            <% end %>
          -->
            <li>
              <a href="tel:6612413335"><span class="glyphicon glyphicon-phone-alt" aria-hidden="true"></span> 661-241-3335</a>
            </li>
          </ul>
        </div><!-- /.navbar-collapse -->
      
      </div><!-- navbar container -->
    </nav>
  
    <div class="container">
      <% flash.each do |type, msg| %>
        <%= content_tag :div, msg, class: "alert alert-#{type}" %>
      <% end %>
    </div>
    
    <%= yield %>
  
    <footer class="footer">
      <div class="container">
        <p class="copyright text-center">© 2017 <strong><%= link_to "Peaches & Clean", root_url %></strong><span class="license">Lic. # 151614</span></p>
      </div>
    </footer>

  </body>
</html>
